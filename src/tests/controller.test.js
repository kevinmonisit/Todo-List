/**
 * function deleteTodo(currentProject, todoInstance) {
 *
 * }
 *
 * //in the project component when the buttons
 * //are being initialized
 *
 *
 *
 * deleteButton.addEventListener('onClick', () => {
 *   deleteClick();
 * });
 *
 */